version: '3.8'

services:
  {{ web_app_name }}:
    image: {{ web_app_image }}:{{ web_app_tag }}
    container_name: {{ web_app_name }}
    ports:
      - "{{ web_app_port_mapping }}"
    # environment:
      # Add environment variables here
      # Use Vault-encrypted secrets
    restart: unless-stopped
    # Add other configuration
