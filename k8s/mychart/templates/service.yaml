apiVersion: v1
kind: Service
metadata:
  name: infoservice-service

spec:
  type: NodePort
  selector:
    app: infoservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
